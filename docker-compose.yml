services:
  postgres:
    image: postgres:18.3-alpine
    environment:
      POSTGRES_DB: user-api
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d user-api"]
      interval: 5s
      timeout: 3s
      retries: 5

  user-api-dev:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/user-api
      SPRING_DATASOURCE_USERNAME: dev
      SPRING_DATASOURCE_PASSWORD: password123
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
